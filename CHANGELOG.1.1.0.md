# simple-unified-application-portal-web 1.1.0 Changelog

## 发布信息

- 版本：`1.1.0`
- 类型：Feature / 向后兼容能力扩展
- 基线版本：`1.0.0`

## 主要变更

- Portal 导航改用 IAM 返回的递归 `menuTree`：目录节点可独立展开或收起，页面节点按服务端路由导航；未配置图标时目录和页面分别回退为文件夹和文档图标。
- 应用根节点可独立收起其菜单；桌面端侧栏可收为一致尺寸的图标轨并记住选择，移动端继续使用抽屉导航。
- Portal 根路由可按 IAM 返回的全局登录首页进入指定应用；应用根路由可进入该应用的默认页面和静态子路径，已指定的深链始终保持原地址。
- PAGE 的 `IMMERSIVE` 展示模式由当前最长匹配路由决定：隐藏 Portal 顶栏和侧栏；进入标准管理页后自动恢复门户壳。
- 新增导航上下文端点的 404 回退到 1.0 平铺应用接口，支持 Portal 先于 IAM Server 1.1 部署。

## 兼容性

| 依赖 | 兼容范围 |
| --- | --- |
| IAM Server | `1.1.x` 完整能力；`1.0.x` 平铺菜单回退 |
| IAM Contract | `1.1.x` |
| IAM Admin Web | 支持递归菜单树、默认入口和展示模式的版本 |
| IAM Theme Contract | `1.0.1` |

## 验证范围

- 单元测试覆盖递归菜单、深链祖先展开、应用和侧栏折叠、默认入口、全局登录首页、沉浸与标准布局切换，以及 1.0 导航接口回退。
- 浏览器测试覆盖生产构建中的应用根节点折叠、图标轨、移动端抽屉、长文案布局和沉浸展示切换。
- 本地真实入口应按本仓 `LOCAL_TEST_COMMANDS.md` 复验 IAM 1.1 的 Portal 导航上下文和可信应用准入链路。
